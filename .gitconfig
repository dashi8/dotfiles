[core]
    excludesfile = ~/.gitignore_global
    editor = vim
[merge]
    tool = vimdiff
[alias]
    s = status
    co = checkout
    g = log --graph --topo-order --all --date=short --pretty=format:'%C(green)%h %C(blue)%ad %C(cyan)%an%C(red)%d %C(reset)%s'
    cm = commit -m
    difff = diff --word-diff
    lol = log --graph --decorate --pretty=oneline --all --abbrev-commit
[user]
	name = dashi8
	email = naoya888kmnth@gmail.com
[include]
    path = ~/.gitconfig.local
